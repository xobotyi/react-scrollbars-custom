import React from "react";

export enum DIRECTION_AXIS {
    X = 1,
    Y = 2,
}

export const DIRECTION_AXIS_X = 1;
export const DIRECTION_AXIS_Y = 2;

export default class Scrollbar extends React.Component {
    update = () => {

    };

    render() {
        return (
            <div />
        );
    }
};
